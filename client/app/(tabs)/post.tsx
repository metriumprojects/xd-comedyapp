import { View } from 'react-native';

/** Tab placeholder — create-post opens via tabPress listener in _layout.tsx */
export default function PostTabPlaceholder() {
  return <View style={{ flex: 1, backgroundColor: '#000' }} />;
}
